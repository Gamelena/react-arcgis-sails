define(["dojo/_base/declare", "react", "react-dom", "react-bootstrap"],
    function (declare, React, ReactDOM, ReactBootstrap) {

        return declare("jsx.UI", null, {
            openForm : function(){
                debugger;

            }
        });
    })